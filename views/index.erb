 

    


      <% if session[:user_id] %>
        <div class="col-md-4">
          
          <div class="welcome-user">


            Welcome, <%= current_user.fname %>
            <br>
              <a href="/users/details/<%= current_user.id %>">Edit Profile</a>
              <a href="/users/delete/<%= current_user.id %>">Delete Account</a>
            
            <form action="/posts" method="post">

        
              <textarea type="text" name="body" id="body"  ></textarea>

                    <br />
                    <br />

              <button type="submit">Create Post</button>
              </form>
          </div>      
             
       </div>

    

      <div class="col-md-4">



        <h1 class="feed-header">FEEDERS</h1>
        <ul class="main-feed">
          <% @posts.each do |post| %>
        <li class="all-posts"><%= post.body %></a> 
        <% if session[:user_id] %>
        <a href="posts/delete/<%= post.id %>">Delete</a>
        <% end %>

          <% end %>
        
      
      </ul>

      </div>

      <div class="col-md-4">
      
      <div class="user-box">
          <h1>User List</h1>
      <ul class="user-list">
          <% @users.each do |user| %>
          <a href="/users/<%= user.id %>"><li class="list-users"><%= user.fname %></li></a>
          <% end %>

      </ul>
      </div>
        
      </div>

      <% else %>
        

        <div class="row">

         <a href="/sign-up">
          <div class="parallax">
             <div class="content">
                <h3>->Click to join the community and grow your fanbase!<-</h3>
                </div>      

         </div>
         </a>

    
          <div class="col-md-6 featured">
              <iframe  class="featured-video" width="560" height="315" src="https://www.youtube.com/embed/O5HiFJxoIS4" frameborder="0" allowfullscreen></iframe>
          </div>

          <div class="col-md-6 featured">
            <h1 class="main-call">Featured Video</h1>
            <h4 class="call-out"> Upload your content today and become feature on the front page. We randomly pick a new feature every week!</h4>

          </div>

        </div>
       
    <% end %>
      



  

   

  


    



 