
  <head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />

    <title>Dylan's Page</title>
  </head>

  <body>
    <h1>Hello, world!</h1>
    <% if current_user %>
      Now logged in as: <%= current_user.fname %>
    <% end %>

  
      <a href="/users/details/<%= session[:user_id] %>">Edit Profile</a>

      <a href="/sign-up">Sign up!</a>
      


    <% if session[:user_id] %>
      <a href="/sign-out">Log Out</a>
    <% else %>
      <a href="/sign_in">Log In</a>
    <% end %>




    <h1>All my posts</h1>
    <ul class="main-feed">
      <% @posts.each do |post| %>
        <li class="all-posts"><%= post.body %></a> 
        
      <% end %>
    </ul>


    <% if session[:user_id] %>
    <form action="/posts" method="post">

      <label for="body">Body</label>
      <textarea type="text" name="body" id="body"  ></textarea>

      <br />
      <br />

      <button type="submit">Create Post</button>
    </form>
  <% end %>

 